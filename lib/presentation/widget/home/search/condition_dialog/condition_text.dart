import 'package:flutter_neumorphic/flutter_neumorphic.dart';

class SearchConditionText extends StatelessWidget {
  const SearchConditionText();

  @override
  Widget build(BuildContext context) {
    return const Text(
      '検索条件',
      style: TextStyle(fontSize: 20),
    );
  }
}
